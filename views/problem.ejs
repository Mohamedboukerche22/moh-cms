<!DOCTYPE html>
<html>
<head>
    <title><%= problem.title %> | IOI Platform</title>
    <link href="/tailwind.min.css" rel="stylesheet">
    <link href="/styles.css" rel="stylesheet">
</head>
<body>
    <nav class="flex justify-center">
        <a href="/">Home</a>
        <a href="/dashboard">Dashboard</a>
        <a href="/submissions">My Submissions</a>
        <% if (user.role === 'admin') { %>
            <a href="/admin/dashboard">Admin Dashboard</a>
        <% } %>
        <a href="/logout">Logout</a>
    </nav>
    <div class="container">
        <h1 class="text-2xl font-bold mb-4"><%= problem.title %></h1>
        <p class="mb-4"><%= problem.description %></p>
        <h2 class="text-xl font-semibold mb-2">Submit Solution</h2>
        <form action="/submit/<%= problem.id %>" method="POST">
            <textarea name="code" rows="10" cols="50" placeholder="Enter your C++ code here" required class="w-full"></textarea>
            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html>